<Window x:Name="MilDBMainWindow" x:Class="MilDB_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="450" Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation
                        Storyboard.TargetProperty="Opacity"
                        From="0" To="1" Duration="0:0:0.5"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Grid>
        <Border x:Name="BGBorder" CornerRadius="20" Background="#FF2E2E2E" Opacity="0.95">
            <Grid>
                <Border x:Name="TopMenuBorder" CornerRadius="20, 20, 0, 0" Height="25" VerticalAlignment="Top" Background="Black" MouseLeftButtonDown="DragWindow">
                    <Grid>
                        <Label HorizontalAlignment="Center" VerticalAlignment="Center" Content="ТАБЛИЦЯ" Foreground="White"></Label>
                        <Border x:Name="CloseProgramButtonBorder" Width="40" Height="25" HorizontalAlignment="Right" VerticalAlignment="Top">
                            <Button Width="40" Height="25" HorizontalAlignment="Right" VerticalAlignment="Top" BorderThickness="0" Click="CloseButton_Click" Style="{StaticResource TopBarButtonStyle}"/>
                        </Border>

                        <Border x:Name="HideButtonBorder" Width="40" Height="25" HorizontalAlignment="Left" VerticalAlignment="Top">
                            <Button Width="40" Height="25" HorizontalAlignment="Left" VerticalAlignment="Top" BorderThickness="0" Click="HideButton_Click" Style="{StaticResource HideTopBarButtonStyle}"/>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Grid Margin="10, 30, 560, 10">
            <Border x:Name="ControlCenterBorder" CornerRadius="20, 0, 0, 20" Background="Azure">
                <Grid>

                    <Button x:Name="CreateObjectButton" VerticalAlignment="Top" Margin="20" Content="СТВОРИТИ" Style="{StaticResource GlobalButtonStyle}" Click="OpenSelectWindow_Click"/>
                    <Button x:Name="Button2" VerticalAlignment="Top" Margin="20, 100, 20, 0" Content="НОВА КНОПКА" Style="{StaticResource GlobalButtonStyle}"/>
                    <Button x:Name="Button3" VerticalAlignment="Center" Margin="20, 0, 20, 0" Content="НОВА КНОПКА" Style="{StaticResource GlobalButtonStyle}"/>
                    <Button x:Name="Button4" VerticalAlignment="Bottom" Margin="20, 0, 20, 20" Content="НОВА КНОПКА" Style="{StaticResource GlobalButtonStyle}"/>
                    <Button x:Name="Button5" VerticalAlignment="Bottom" Margin="20, 0, 20, 100" Content="НОВА КНОПКА" Style="{StaticResource GlobalButtonStyle}"/>
                </Grid>
            </Border>
        </Grid>

        <Grid Margin="245, 30, 10, 10">
            <Border x:Name="TableInfo" CornerRadius="0, 20, 20, 0" Background="Azure"></Border>
            <Grid Margin="5, 5, 20, 5">

            </Grid>
        </Grid>
    </Grid>
</Window>
