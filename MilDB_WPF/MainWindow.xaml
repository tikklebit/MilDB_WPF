<Window x:Name="MilDBMainWindow" x:Class="MilDB_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MilDB_WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation
                        Storyboard.TargetProperty="Opacity"
                        From="0" To="1" Duration="0:0:0.5"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Grid>
        <Border x:Name="BGBorder" CornerRadius="20" Background="#FF2E2E2E" Opacity="0.95">
            <Grid>
                <Border x:Name="TopMenuBorder" CornerRadius="20, 20, 0, 0" Height="25" VerticalAlignment="Top" Background="Black" MouseLeftButtonDown="DragWindow">
                    <Grid>
                        <Label HorizontalAlignment="Center" VerticalAlignment="Center" Content="ТАБЛИЦЯ" Foreground="White"></Label>
                        <Border x:Name="CloseProgramButtonBorder" Width="40" Height="25" HorizontalAlignment="Right" VerticalAlignment="Top">
                            <Button Width="40" Height="25" HorizontalAlignment="Right" VerticalAlignment="Top" BorderThickness="0" Click="CloseButton_Click">
                                <Button.Background>
                                    <ImageBrush ImageSource="/closeprogrambutton.png"/>
                                </Button.Background>
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="0,20,0,0">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Border>

                        <Border Width="40" Height="25" HorizontalAlignment="Left" VerticalAlignment="Top">
                            <Button Content="-" Width="40" Height="25" HorizontalAlignment="Left" VerticalAlignment="Top" BorderThickness="0" Click="HideButton_Click">
                                <Button.Background>
                                    <ImageBrush ImageSource="/hidewindowbutton.png"/>
                                </Button.Background>
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="20,0,0,0">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Label Content="Головна сторінка" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="30" Foreground="White" Margin="0,28,0,0"></Label>

        <Grid Margin="19, 40, 581, 15">
            <Border x:Name="NewObjectsButtonsBorder" CornerRadius="20" Background="Azure">
                <Grid>

                    <Button x:Name="NewConscriptButton" VerticalAlignment="Top" Content="СТВОРИТИ" Background="#141516" Foreground="White">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="#141516" CornerRadius="20" Padding="0, 20, 0, 20" Margin="10, 20, 10, 0">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="NewOfficerButton" VerticalAlignment="Center" Content="НОВИЙ ОФІЦЕР" Background="#141516" Foreground="White">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="#141516" CornerRadius="20" Padding="0, 20, 0, 20" Margin="10, 0, 10, 0">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="NewMilitaryButton" VerticalAlignment="Bottom" Content="НОВЕ ТЦК" Background="#141516" Foreground="White">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="#141516" CornerRadius="20" Padding="0, 20, 0, 20" Margin="10, 0, 10, 20">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                </Grid>

            </Border>
        </Grid>

        <Grid Margin="250, 40, 19, 15">
            <Border x:Name="TableInfo" CornerRadius="20" Background="Azure"></Border>
        </Grid>
    </Grid>
</Window>